#:kivy 1.0.9

<MainFrame>:
    Splitter:
        sizable_from: 'right'
        #keep_within_parent: in_parent_box.active
        #rescale_with_parent: rescale_box.active
        Display:
    RightPane:


<RightPane>:
    Button:
        text: 'From kv file'

<Display>:
    canvas:
        Color:
            rgb: .5, .5, .5
        Rectangle:
            pos: self.pos
            size: self.size
